public class Book extends LibraryItem{

    // constructor for Book class
    public Book(String id, String title, String section, boolean onHand, boolean reserved, String author, String publishingCompany) {

        super(id, title, section, onHand, reserved);

        
        
    }
    
}