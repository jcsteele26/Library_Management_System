public class Audio extends LibraryItem{

    // constructor for Audio class
    public Audio(String id, String title, String section, boolean onHand, boolean reserved) {

        super(id, title, section, onHand, reserved);
    }
    
}